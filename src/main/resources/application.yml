spring:
  application:
    name: quoraapp
  data:
    mongodb:
      host: localhost
      port: 27017
      database: quora_db
      auto-index-creation: true
    elasticsearch:
      uris: http://localhost:9200

kafka:
  bootstrap-servers: localhost:9092

elasticsearch:
  host: localhost
  port: 9200
  scheme: http